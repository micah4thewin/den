{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The default capability for the Play shell: IPC to the local commands only.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
